require 'rake/clean'

desc "generate files"
task :site => :assets do
  
end

task :assets => :directories do

end

taks :directories do
  puts "Creating directories..."
  
end

task :default => 'site'